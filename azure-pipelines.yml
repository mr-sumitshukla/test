trigger:
- staging

resources:
- repo: self

stages:
- stage: mujhe_dekhna_hai_ki
  displayName: pipeline_kaise_kaam_karti_hai
  jobs:
  - job: Build
    displayName: command_chalana_hai
    pool:
      vmImage: ubuntu-latest
      name: Default
    steps:
    - task: SSH@0
      inputs:
        sshEndpoint: 'demo-lokesh-1-ssh'
        runOptions: 'commands'
        commands: "cd /home/azureuser && ls"
        readyTimeout: '20000'
