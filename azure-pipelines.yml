trigger:
- staging

resources:
- repo: self

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
      name: Default
    steps:
    - task: SSH@0
      inputs:
        sshEndpoint: 'demo-lokesh-1-ssh'
        runOptions: 'commands'
        commands: "cd /home/azureuser && ls"
        readyTimeout: '20000'
